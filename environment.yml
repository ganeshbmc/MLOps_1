name: iris_env_c
channels:
  - conda-forge
dependencies:
  - python=3.10
  - dvc
  - joblib
  - scikit-learn
  - pandas
  - pip
  - pip:
      - pytest>=7.0
      - cml==0.10.5
prefix: /opt/conda/envs/iris_env_c
