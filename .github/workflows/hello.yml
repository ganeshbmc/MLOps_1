me: Hello Workflow

on: [push]

jobs:
    say_hello:
          runs-on: ubuntu-latest
          steps:
                - name: Print greeting
                  run: echo "Hello from GitHub Actions!"

